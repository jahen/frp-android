<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="max.plus.frp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>

    <!--存储权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application android:name="max.plus.frp.FrpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name="max.plus.frp.ui.LogcatActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name="max.plus.frp.ui.IniEditActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name="max.plus.frp.ui.MainActivity"
            android:launchMode="singleTop"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="max.plus.frp.ui.TemplateActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name="max.plus.frp.ui.VersionActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name="max.plus.frp.ui.AboutActivity"
            android:label="@string/app_repository" />

        <service android:name="max.plus.frp.FrpcService" />
        <service android:name="max.plus.frp.FrpsService"
            android:exported="false"
            android:process=":frps" />
    </application>

</manifest>